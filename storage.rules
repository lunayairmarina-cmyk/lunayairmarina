rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Storage is intentionally unused (cost). Images are stored in Firestore as compressed data URLs.
    match /{allPaths=**} {
      allow read: if false;
      allow write: if false;
    }
  }
}
